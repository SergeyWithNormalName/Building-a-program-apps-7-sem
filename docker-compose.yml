services:
  web:
      image: nginx:alpine
      ports:
        - "8080:80"
      depends_on:
        - postgres
      networks:
        - my-app-network

  postgres:
      image: postgres:16
      environment: 
        - POSTGRES_PASSWORD=password

networks:
    my-app-network: